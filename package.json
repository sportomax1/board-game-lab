{
  "name": "bgg-proxy-app",
  "version": "1.0.0",
  "description": "A secure Vercel proxy for the BGG API.",
  "private": true,
  "main": "api/bgg-proxy.js",

  "scripts": {
    "start": "vercel dev",
    "deploy": "vercel",
    "proxy": "echo 'Running proxy function'"
  },

  "dependencies": {
    "@vercel/analytics": "^1.6.1",
    "dotenv": "^17.2.3"
  }
}